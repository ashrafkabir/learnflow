# LearnFlow local helpers

## rg

This repo sometimes references ripgrep (`rg`). If you don't have it installed system-wide, run it via:

- `./rg <args>` (wrapper; uses @vscode/ripgrep)

Or install ripgrep on your OS.\n
