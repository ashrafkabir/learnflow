{
  "name": "Tutor Agent",
  "version": "0.1.0",
  "description": "Deterministic, safe general Q&A tutoring fallback that works without BYOAI keys.",
  "capabilities": ["tutor_qa"],
  "requiredProviders": [],
  "inputSchema": {
    "type": "object",
    "properties": {
      "type": { "type": "string" },
      "input": { "type": "string" }
    },
    "required": ["input"]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "text": { "type": "string" },
      "actions": { "type": "array", "items": { "type": "string" } }
    },
    "required": ["text"]
  }
}
