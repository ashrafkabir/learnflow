# Iteration 91 — Planner Plan (Spec vs Implementation)

Date: 2026-03-25 (America/New_York)
Planner: Ash (planner subagent)

## Objective

Bias Iter91 toward the **largest remaining mismatches** against `LearnFlow_Product_Spec.md` (v1.0, March 2026), focusing on **agent transparency + orchestration UX**, **source/provenance integrity**, and **Update Agent / notifications reality**.

Constraints for planner run:

- Doc-only changes (no product code).
- Evidence-based: point to current code + screenshot artifacts.

## Evidence produced this run

- Screenshot sweep dir: `learnflow/screenshots/iter91/planner-run/`
  - Notes: `learnflow/screenshots/iter91/planner-run/NOTES.md`

## Current reality (high-signal)

### What’s strong (relative to spec)

- **Lesson Reader** is feature-rich and already exceeds spec in places:
  - Action chips + sources drawer implemented.
  - Inline “selection tools” preview + attach flows exist.
  - Illustration metadata includes attribution fields (provider/license/source URL).
- **Agent packages** include `manifest.json` + `initialize/process/cleanup` expectations (partial SDK resemblance).
- **API** stores usage records and has per-agent/provider attribution (best-effort).

### Biggest spec mismatches (still)

1. **Orchestrator + multi-agent DAG planner** (Spec §4): implemented as a pragmatic orchestrator call path, but not a real DAG planner/parallel execution model with a standardized envelope.
2. **Agent transparency in UX** (Spec §5.2.3): some indicators exist, but there’s no consistent “which agent is working and why” across screens.
3. **Update Agent (Pro) proactive monitoring** (Spec §14): still MVP; notifications generation is largely non-crawling / generic.
4. **Attribution model + credibility index** (Spec §6.3): structured sources exist, but no credibility scoring and no full provenance chain.
5. **Subscription/Pro** flow messaging: onboarding literally says “Pro Coming Soon!” while pricing page implies upgrade works.

## Iter91 Task Backlog (10–15) with acceptance criteria

> Labels:
>
> - **P0** = must-do to reduce major spec mismatch / unblock demos
> - **P1** = should-do to close key gaps
> - **P2** = nice-to-have / hygiene

### P0 — Agent transparency + orchestration UX coherence

1. **Conversation: consistent “Agent activity” indicator + provenance of responses**

- Problem (Spec §5.2.3): spec expects subtle agent activity indicator (which agent + why). Current Conversation screen receives WS events but visibility is inconsistent.
- Acceptance criteria:
  - Conversation shows an “Agent working…” strip that updates on `agent.spawned/agent.complete` and includes agent display name + short reason.
  - On completion, each assistant message can optionally show “Generated by: <agent>” and “Sources used: N”.
- Likely files:
  - `apps/client/src/screens/Conversation.tsx`
  - `apps/api/src/websocket.ts` (if event payload needs minor enrich)
  - `packages/core/src/*` (event typing)
- Screenshots:
  - `app-conversation` with visible agent activity.
- Verification:
  - UI test (RTL) asserting indicator renders for spawned events.

2. **Standardize agent naming and capability disclosure across UI**

- Problem (Spec §4.2/§7.2): agent marketplace + conversation + settings should use consistent labels.
- Acceptance criteria:
  - One shared mapping function for agent names (avoid duplicates between API and client).
  - Agent cards display capability tags that match manifest.
- Likely files:
  - `apps/client/src/screens/marketplace/AgentMarketplace.tsx`
  - `packages/agents/src/*/manifest.json`
  - `apps/api/src/routes/chat.ts` (currently `formatAgentName` exists)

3. **Implement a minimal “Message Envelope” doc + types (even if not full gRPC)**

- Problem (Spec §4.3): there’s no explicit envelope schema documented/typed for internal agent calls.
- Acceptance criteria:
  - Add a `docs` section describing the envelope used today (ids, from/to, context slice, task params, timeout).
  - Add TS types in `packages/core` and align orchestrator usage.
- Likely files:
  - `packages/core/src/agents/types.ts`
  - `apps/docs/pages/*` or new `apps/docs/pages/architecture.md`

### P0 — Update Agent realism + notifications

4. **Update Agent: document current behavior + explicitly mark “non-crawling” MVP**

- Problem (Spec §14): current implementation is not actually monitoring feeds/web.
- Acceptance criteria:
  - Add a visible disclaimer in Settings “Update Agent” panel describing MVP behavior.
  - Add roadmap bullets mapping to spec sections.
- Likely files:
  - `apps/client/src/components/update-agent/UpdateAgentSettingsPanel.tsx`
  - `IMPLEMENTED_VS_SPEC.md` (refresh to Iter91)
- Screenshots:
  - `app-settings` with Update Agent section.

5. **Add /update-agent/topics API contract completeness (status fields)**

- Problem: queue already calls out missing run metadata.
- Acceptance criteria:
  - `GET /api/v1/update-agent/topics` returns `lastRunAt`, `lastRunOk`, `lastRunError`, and `lockedAt/lockId` if present.
  - Test coverage.
- Likely files:
  - `apps/api/src/routes/update-agent.ts`
  - `apps/api/src/db.ts`

### P0 — Sourcing & provenance trust

6. **Credibility scoring (lightweight) + surface in sources drawer**

- Problem (Spec §6.3): credibility index is absent.
- Acceptance criteria:
  - Add an MVP `credibilityScore` (heuristic) per source (domain reputation / sourceType / recency).
  - Surface score label in AttributionDrawer (dev-mode acceptable).
- Likely files:
  - `apps/api/src/utils/sourcesStructured.ts` or `sourceCards.ts`
  - `apps/client/src/components/AttributionDrawer.tsx`

7. **Deduplication + provenance chain doc**

- Problem (Spec §6.1/§6.3): dedupe and provenance chain are not explicit.
- Acceptance criteria:
  - Document current dedupe behavior (if any) and gaps.
  - Add an issue/task to implement durable provenance chain (source→lesson→sections→citations).
- Likely files:
  - `packages/agents/src/content-pipeline/*`
  - `apps/api/src/utils/*`

### P1 — Subscription & upgrade coherence

8. **Fix contradictory Pro messaging**

- Problem: onboarding says “Pro Coming Soon!” but pricing triggers `/subscription upgrade`.
- Acceptance criteria:
  - Align copy and behavior. If Pro is supported: remove “coming soon” modal.
  - If Pro is not supported: disable upgrade button and show clear CTA.
- Likely files:
  - `apps/client/src/screens/onboarding/SubscriptionChoice.tsx`
  - `apps/client/src/screens/marketing/Pricing.tsx`
  - `apps/api/src/routes/subscription.ts`

9. **Usage dashboard parity (per-agent token counts)**

- Problem (Spec §4.4): token usage is expected; API stores minimal usage but UI likely incomplete.
- Acceptance criteria:
  - Settings shows provider usage per agent with last 7/30d totals.
- Likely files:
  - `apps/client/src/screens/ProfileSettings.tsx`
  - `apps/api/src/routes/analytics.ts` (or new route)

### P1 — Collaboration: remove misleading “Coming soon” or implement minimal shared mindmaps

10. **Collaboration: either implement shared mindmaps MVP or clearly scope it out**

- Acceptance criteria:
  - If not implemented: replace “Coming soon” block with a roadmap mapped to spec.
  - If implemented: show a list of shared mindmaps and a join/view flow.
- Likely files:
  - `apps/client/src/screens/Collaboration.tsx`
  - `apps/api/src/routes/collaboration.ts`

### P2 — Docs, QA, artifact hygiene

11. **Update IMPLEMENTED_VS_SPEC to Iter91 reality**

- Acceptance criteria:
  - Update section statuses + cite current evidence (e.g., LessonReader sources drawer, usage records).
- Likely files:
  - `IMPLEMENTED_VS_SPEC.md`

12. **OneDrive sync checklist (planner artifacts)**

- Acceptance criteria:
  - Add explicit sync steps/command notes and required paths.
- Paths:
  - `learnflow/screenshots/iter91/planner-run/`
  - `learnflow/IMPROVEMENT_QUEUE.md`
  - `learnflow/ITERATION_91_PLAN.md`

13. **Screenshot harness: ensure it captures critical spec proof states**

- Acceptance criteria:
  - Harness opens sources drawer in LessonReader and captures screenshot.
  - Harness triggers an agent activity state in Conversation and captures screenshot.
- Likely files:
  - `learnflow/screenshot-all.mjs` or `scripts/screenshots*.js`

14. **API OpenAPI / docs parity for Update Agent + Notifications**

- Acceptance criteria:
  - OpenAPI includes update-agent endpoints and notifications generate endpoint.
- Likely files:
  - `apps/api/openapi.yaml`
  - `apps/docs/pages/api.md`

15. **Rate limiting + caching docs**

- Acceptance criteria:
  - Document current rate limiting posture and known limitations.
- Likely files:
  - `apps/api/src/rateLimit.ts`
  - `apps/docs/pages/security.md`

## Screenshot checklist (Iter91)

- `app-conversation` — agent activity visible
- `lesson-reader` — sources drawer open with credibility/whyThisMatters
- `app-settings` — Update Agent disclaimers/status fields
- `onboarding-5-subscription` — consistent Pro messaging
- `app-collaboration` — no misleading “coming soon” without roadmap

## Verification checklist (Iter91)

- `npm test`
- `npx tsc --noEmit`
- `npx eslint .`
- `npx prettier --check .`

## OneDrive sync (TODO)

Planner run did not sync. Required sync paths:

- `/home/aifactory/.openclaw/workspace/learnflow/screenshots/iter91/planner-run/`
- `/home/aifactory/.openclaw/workspace/learnflow/ITERATION_91_PLAN.md`
- `/home/aifactory/.openclaw/workspace/learnflow/IMPROVEMENT_QUEUE.md`
